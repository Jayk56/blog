# This .gitignore is appropriate for repositories deployed to GitHub Pages and using
# a Gemfile as specified at https://github.com/github/pages-gem#conventional

# Basic Jekyll gitignores (synchronize to Jekyll.gitignore)
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Hugo specific ignores
# Hugo will create a public directory for the static site
# This is the default output directory for Hugo
# If you are using a different output directory, change this line
public/
# Hugo will create a cache directory for the static site
# This is the default cache directory for Hugo
# If you are using a different cache directory, change this line
cache/
# Hugo will create a resources directory for the static site
# This is the default resources directory for Hugo
# If you are using a different resources directory, change this line
resources/
# Hugo will create a vendor directory for the static site
# This is the default vendor directory for Hugo
# If you are using a different vendor directory, change this line
vendor/
# Hugo will create a modules directory for the static site
# This is the default modules directory for Hugo
# If you are using a different modules directory, change this line
modules/
# Hugo will create a themes directory for the static site
# This is the default themes directory for Hugo
# If you are using a different themes directory, change this line
# themes/
# Hugo will create a config.toml file for the static site
# This is the default config.toml file for Hugo
# If you are using a different config.toml file, change this line
config.toml


# Additional Ruby/bundler ignore for when you run: bundle install
/vendor

# Specific ignore for GitHub Pages
# GitHub Pages will always use its own deployed version of pages-gem 
# This means GitHub Pages will NOT use your Gemfile.lock and therefore it is
# counterproductive to check this file into the repository.
# Details at https://github.com/github/pages-gem/issues/768
Gemfile.lock

# Playwright artifacts
.playwright-mcp/

# Hugo lockfile
jkerschner.com/.hugo_build.lock

# Audio files (too large for git — keep locally or in iCloud)
audio-notes/**/*.m4a
audio-notes/**/*.mp3
audio-notes/**/*.wav
audio-notes/**/*.webm
audio-notes/**/*.mp4

# Pipeline secrets
pipeline/.env

# Per-file transcription JSON (raw Scribe output, large)
output/transcribe/**/*.json

# Node.js dependencies (Playwright for collect stage)
pipeline/node_modules/

# Collected assets (screenshots are large binary files)
output/collect/**/assets/*.png
output/collect/**/assets/*.jpg

# Collection logs
output/collect/**/collect.log

# Blog editor app
editor/node_modules/
editor/dist/

# Codex CLI (contains API keys)
.codex/

# macOS junk
.DS_Store
